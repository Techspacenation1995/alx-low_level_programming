#include <stdio.h>

/**
 * main - Printing out to standard output using the printf function
 *
 * Return: return 0 if successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
